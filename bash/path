if [ -f ~/.path ]; then
    for path in $(cat ~/.path); do
        export PATH="$PATH:$path"
    done
fi

if [ -d ~/.path.d ]; then
    for path in $(cat ~/.path.d/*); do
        export PATH="$PATH:$path"
    done
fi
